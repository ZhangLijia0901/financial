spring:
  application:
    name: register-center
server:
  port: 9000
logging:
  level:
    root: info
  file: F:/github/logs/financial/register-center.log
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/